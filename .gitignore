node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.antigravity/
dev_db.json
prisma/dev.db
*.db
*.db-journal
.vercel

# Local database snapshots taken before schema changes — not source
dev.db.bak-*
prisma/dev.db.bak-*

# Google Calendar secret iCal feed — a read credential for the private calendar.
# Server-side only; never committed, never sent to the browser.
.calendar-feed.json

# Generated data exports — the whole ledger as a spreadsheet; data, not source
exports/

# Editor-made backups of scripts during changes
*.bak-*
